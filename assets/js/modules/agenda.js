import {STATE} from '../core/state.js?v=546119';
const $=(id)=>document.getElementById(id);
const setText=(id,v)=>{const e=$(id); if(e) e.textContent=v??''};
const setHTML=(id,v)=>{const e=$(id); if(e) e.innerHTML=v??''};
const on=(id,ev,fn)=>{const e=$(id); if(e) e.addEventListener(ev,fn)};
let viewDate=new Date();
export function mountAgenda(){const el=$('mod-agenda');if(!el)return;el.innerHTML=`<div class="agenda-body"><div class="agenda-main"><div class="cal-nav"><button class="cal-btn" id="calPrev">‹</button><div class="cal-month" id="calMonth">Agenda</div><button class="cal-btn" id="calNext">›</button></div><div class="cal-grid" id="calGrid"></div><div class="premium-card" style="margin-top:1rem"><div class="sec-title"><span class="sec-icon">🧾</span><span class="sec-lbl">OS / Serviços agendados</span></div><div id="agendaLista" class="premium-list"></div></div></div><aside class="agenda-side"><div class="sec-lbl">Etapas automáticas</div><div class="premium-list" style="margin-top:.7rem"><div class="premium-item info"><div><div class="item-title">Desmontagem</div><div class="item-sub">Início técnico da OS</div></div></div><div class="premium-item info"><div><div class="item-title">Funilaria</div><div class="item-sub">Reparo estrutural/lataria</div></div></div><div class="premium-item info"><div><div class="item-title">Preparação/Pintura</div><div class="item-sub">Acabamento e cabine</div></div></div><div class="premium-item info"><div><div class="item-title">Montagem/Entrega</div><div class="item-sub">Finalização e checklist</div></div></div></div><div class="alert" style="margin-top:1rem">Eventos nascem do orçamento aprovado. A Agenda não calcula valores; ela só reflete OS e prazos.</div></aside></div>`;on('calPrev','click',()=>{viewDate.setMonth(viewDate.getMonth()-1);renderAgenda()});on('calNext','click',()=>{viewDate.setMonth(viewDate.getMonth()+1);renderAgenda()});}
export function renderAgenda(){if(!document.getElementById('calMonth')) mountAgenda(); if(!document.getElementById('calMonth')) return;const clientes=STATE.clientes.lista||[];const y=viewDate.getFullYear(),m=viewDate.getMonth();setText('calMonth',viewDate.toLocaleDateString('pt-BR',{month:'long',year:'numeric'}).toUpperCase());const first=new Date(y,m,1);const start=new Date(y,m,1-first.getDay());const days=['DOM','SEG','TER','QUA','QUI','SEX','SÁB'];let html=days.map(d=>`<div class="cal-day-hdr">${d}</div>`).join('');for(let i=0;i<42;i++){const d=new Date(start);d.setDate(start.getDate()+i);const iso=d.toISOString().slice(0,10);const ev=(STATE.agenda.eventos||[]).filter(e=>e.data===iso);const today=iso===new Date().toISOString().slice(0,10);html+=`<div class="cal-day ${today?'today':''} ${ev.length?'has-job':''}"><div class="cal-day-num">${d.getDate()}</div>${ev.slice(0,3).map(e=>`<div class="cal-job-dot">${e.titulo}</div>`).join('')}</div>`}setHTML('calGrid',html);setHTML('agendaLista',(STATE.agenda.eventos||[]).map(e=>{const c=clientes.find(x=>x.id===e.clienteId);return `<div class="premium-item ${e.prioridade==='Urgente'?'danger':'info'}"><div><div class="item-title">${e.titulo}</div><div class="item-sub">${e.data} · ${c?.nome||''} · ${e.status}</div></div><span class="status-pill sp-pend">OS</span></div>`}).join('')||'<p class="muted">Sem eventos.</p>');}
