# OficinaOS V601.11 — 7 Abas + IndexedDB

Abra `index.html` por um servidor local ou publique a pasta inteira.

Exemplo:

```bash
python -m http.server 8080
```

Depois abra:

`http://localhost:8080`

Persistência oficial: IndexedDB.


## V601.11

Removidos menus internos duplicados. A navegação oficial fica somente no index principal.
