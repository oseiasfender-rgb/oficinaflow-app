const assert=require('assert');require('../core700/state.js');require('../core700/events.js');require('../core700/ids.js');const c=OficinaOSIds700.next('CLI'),o=OficinaOSIds700.next('ORC'),s=OficinaOSIds700.next('OS'),f=OficinaOSIds700.next('FIN');OficinaOSState700.update(d=>{d.clientes.push({id:c});d.orcamentos.push({id:o});d.agenda.os.push({id:s});d.financeiro.lancamentos.push({id:f});d.metas.faturamento=1000;return d});const st=OficinaOSState700.get();assert.equal(st.clientes.length,1);assert.equal(st.orcamentos.length,1);assert.equal(st.agenda.os.length,1);assert.equal(st.financeiro.lancamentos.length,1);assert.equal(st.metas.faturamento,1000);console.log('✅ V700.30 integração de STATE: OK');